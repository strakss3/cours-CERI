#include "mot.h"

using namespace std;


Mot::Mot() {
	
	(*this).mot = "" ;
	(*this).nbs = 0 ;

}

/*
Mot::Mot(string m, int size, string *tab) {
	
	(*this).mot = m ;
	(*this).nbs = size ;
	(*this).synonymes = new string[size];
	
	for (int i = 0 ; i < size ; i++) 
	{
		(*this).synonymes[i] = tab[i] ;
	}
}
*/

